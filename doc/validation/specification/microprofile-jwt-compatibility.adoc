= MicroProfile JWT Auth 2.1 Compatibility
:toc: left
:toclevels: 3
:toc-title: Table of Contents
:sectnums:
:source-highlighter: highlight.js

xref:../architecture.adoc[Back to Architecture Reference]

== Overview

This document describes TokenSheriff's relationship to the https://download.eclipse.org/microprofile/microprofile-jwt-auth-2.1/microprofile-jwt-auth-spec-2.1.html[Eclipse MicroProfile JWT Auth 2.1] specification. TokenSheriff implements select MP-JWT 2.1 primitives as a compatibility layer while retaining its advanced multi-issuer, multi-token-type architecture.

=== Document Navigation

* link:../../../README.md[README] - Project overview and introduction
* xref:../../../token-sheriff-validation/README.adoc[Usage Guide] - How to use the library with code examples
* xref:../requirements.adoc[Requirements] - Functional and non-functional requirements
* xref:../architecture.adoc[Architecture] - Architecture reference and implementation details
* xref:token-decryption.adoc[Token Decryption] - JWE decryption specification
* xref:multi-idp-testing.adoc[Multi-IDP Testing] - Multi-IDP integration testing

==== Status: IMPLEMENTED

== Design Rationale

TokenSheriff is **not** an MP-JWT implementation. It is a purpose-built, production-grade JWT/OAuth 2.0/OpenID Connect token validation library that provides capabilities beyond what MP-JWT defines. Rather than conforming to the MP-JWT specification's single-issuer, single-token-type model, TokenSheriff adopts **select primitives** from the spec where they provide value.

The approach is:

* **Core types**: `TokenContent` is framework-agnostic — no dependency on `JsonWebToken`
* **Quarkus adapter**: `JsonWebTokenAdapter` wraps `TokenContent` to provide the `JsonWebToken` interface for CDI injection
* **Clean API separation**: Core uses `Optional`-returning accessors; the adapter bridges to MP-JWT's nullable contract
* **TokenSheriff extensions**: Multi-issuer support, DPoP, custom claim mappers, sealed token hierarchy, security event tracking — all preserved as additional API surface

== Adopted Features

=== `JsonWebToken` Interface (`org.eclipse.microprofile.jwt.JsonWebToken`)

The Quarkus module provides a `JsonWebTokenAdapter` that wraps `TokenContent` and implements `JsonWebToken`, making it available as a `java.security.Principal`. This enables standard Jakarta Security integration:

[source,java]
----
@Inject JsonWebToken callerPrincipal;  // MP-JWT standard injection via adapter
@Inject Principal principal;            // Jakarta Security standard injection
----

The adapter bridges between TokenSheriff's clean API and the MP-JWT spec signatures:

[cols="3,2,2"]
|===
|JsonWebToken Method |Core TokenContent API |Notes

|`getName()`
|`getClaimOption(UPN)` / `getSubject()`
|UPN fallback: `upn` -> `preferred_username` -> `sub`

|`getSubject()`
|`getSubject()` -> `Optional<String>`
|Adapter returns nullable; core returns Optional

|`getExpirationTime()`
|`getExpirationDateTime()` -> `OffsetDateTime`
|Adapter returns epoch seconds; core returns rich type

|`getIssuedAtTime()`
|`getIssuedAtDateTime()` -> `OffsetDateTime`
|Adapter returns epoch seconds; core returns rich type

|`getGroups()`
|`AccessTokenContent.getGroups()`
|Only on concrete type in core

|`getAudience()`
|`AccessTokenContent.getAudience()` / `IdTokenContent.getAudience()`
|Only on concrete types in core

|`getClaimNames()`
|`getClaimNames()`
|Same signature on both

|`getClaim(String)`
|`getClaimOption(ClaimName)`
|Adapter provides typed conversion
|===

=== `upn` Claim (User Principal Name)

The `upn` claim is MP-JWT's standardized human-readable principal identifier. TokenSheriff's `ClaimName` enum includes `UPN` with the spec-defined fallback chain for `Principal.getName()`:

1. `upn` claim (if present)
2. `preferred_username` claim (fallback)
3. `sub` claim (final fallback)

=== Clock Skew Tolerance (`mp.jwt.verify.clock.skew`)

Clock skew tolerance is applied to both `exp` and `nbf` validation, accommodating clock drift in distributed systems. A token is considered expired only when `exp + clockSkew < now`.

Configured per issuer via `sheriff.token.issuers.<name>.clock-skew-seconds` (default: 60 seconds).

=== Token Age Verification (`mp.jwt.verify.token.age`)

Maximum token age validation based on `iat` claim. When enabled, rejects tokens where `now - iat > maxTokenAgeSeconds + clockSkewSeconds`, even if not yet expired.

Configured per issuer via `sheriff.token.issuers.<name>.max-token-age-seconds` (default: disabled).

=== CDI Producers (`@Produces JsonWebToken`, `@Produces Principal`)

Standard MP-JWT CDI injection is supported:

[source,java]
----
@Inject JsonWebToken callerPrincipal;  // MP-JWT standard
@Inject Principal principal;            // Jakarta Security standard
----

When no valid token is present, an empty `JsonWebToken` is injected (per MP-JWT spec).

=== Cookie-Based Token Extraction (`mp.jwt.token.header` / `mp.jwt.token.cookie`)

Alternative token extraction from HTTP cookies instead of the `Authorization` header. Useful for browser-based SPAs using SameSite cookie protection.

Configuration:

* `sheriff.token.token.header` — `Authorization` (default) or `Cookie`
* `sheriff.token.token.cookie-name` — cookie name (default: `Bearer`)

When `sheriff.token.token.header=Cookie`, the token is extracted from the named cookie in the `Cookie` HTTP header. If a valid `Authorization: Bearer <token>` header is also present, it takes precedence.

== Not Adopted (By Design)

[cols="2,3"]
|===
|MP-JWT Feature |Rationale

|`@RolesAllowed` annotation
|TokenSheriff uses `@BearerAuth` which provides superior multi-dimensional authorization (scopes AND roles AND groups). The standard `@RolesAllowed` only checks one dimension.

|`@Claim` CDI qualifier injection
|TokenSheriff's programmatic `getClaimOption(ClaimName)` with custom `ClaimMapper` pipeline provides type-safe, extensible claim access. `@Claim` injection is less flexible for custom claim types and multi-issuer scenarios.

|`@LoginConfig` annotation
|Not applicable to Quarkus extension model. TokenSheriff activates via `@BearerAuth` interceptor binding and configuration properties.

|`mp.jwt.*` config namespace
|`sheriff.token.*` is the established convention. TokenSheriff's per-issuer configuration model (`sheriff.token.issuers.<name>.*`) has no equivalent in the single-issuer `mp.jwt.*` namespace.

|`ClaimValue<T>` (MP-JWT version)
|TokenSheriff's `ClaimValue` with `ClaimMapper` pipeline (e.g., `ScopeMapper`, `KeycloakDefaultRolesMapper`) is more powerful. MP-JWT's `ClaimValue` is a simple typed wrapper.

|Single-issuer model
|TokenSheriff's multi-issuer `IssuerConfigCache` with per-issuer JWKS loading, algorithm constraints, and claim validation is a core differentiator. MP-JWT assumes one issuer per application.
|===

== TokenSheriff Advantages Beyond MP-JWT

The following capabilities have no equivalent in MP-JWT 2.1:

* **Multi-issuer support** — `IssuerConfigCache` with async JWKS loading per issuer
* **DPoP (RFC 9449)** — Optional Demonstrating Proof-of-Possession validation
* **Custom claim mappers** — Pluggable `ClaimMapper` interface with Keycloak-specific implementations
* **Custom validation rules** — Pluggable `TokenValidationRule` interface for domain-specific post-validation checks (e.g., tenant-id enforcement, subject blocklists)
* **Sealed token hierarchy** — `AccessTokenContent`, `IdTokenContent`, `UnvalidatedRefreshToken` with type-safe access patterns
* **Security event tracking** — `SecurityEventCounter` with categorized validation failure tracking
* **Performance monitoring** — `TokenValidatorMonitor` with microsecond-precision pipeline measurements
* **Token caching** — `AccessTokenCache` with TTL-based eviction
* **JWE transparency** — Encrypted tokens decrypted transparently before validation
* **Algorithm security** — RFC 8725 and NIST SP 800-131A compliant algorithm whitelisting

== Configuration Mapping

[cols="2,2,1"]
|===
|MP-JWT Property |TokenSheriff Equivalent |Notes

|`mp.jwt.verify.issuer`
|`sheriff.token.issuers.<name>.issuer-identifier`
|Per-issuer in TokenSheriff

|`mp.jwt.verify.audiences`
|`sheriff.token.issuers.<name>.expected-audience`
|Per-issuer in TokenSheriff

|`mp.jwt.verify.publickey.location`
|`sheriff.token.issuers.<name>.jwks.http.well-known-url`
|TokenSheriff uses OIDC discovery or direct JWKS URL

|`mp.jwt.verify.publickey.algorithm`
|`sheriff.token.issuers.<name>.algorithm-preferences`
|Per-issuer, multiple algorithms supported

|`mp.jwt.verify.clock.skew`
|`sheriff.token.issuers.<name>.clock-skew-seconds`
|Per-issuer in TokenSheriff

|`mp.jwt.verify.token.age`
|`sheriff.token.issuers.<name>.max-token-age-seconds`
|Per-issuer in TokenSheriff

|`mp.jwt.token.header`
|`sheriff.token.token.header`
|Global setting

|`mp.jwt.token.cookie`
|`sheriff.token.token.cookie-name`
|Global setting

|`mp.jwt.decrypt.key.location`
|`sheriff.token.jwe.decryption-key-path`
|JWE decryption key (PKCS#8 PEM; keystore-based configuration also supported)

|`mp.jwt.decrypt.key.algorithm`
|`sheriff.token.jwe.key-management-algorithms`
|JWE key management algorithms (list)
|===

== References

* https://download.eclipse.org/microprofile/microprofile-jwt-auth-2.1/microprofile-jwt-auth-spec-2.1.html[MicroProfile JWT Auth 2.1 Specification]
* https://datatracker.ietf.org/doc/html/rfc7519[RFC 7519 - JSON Web Token (JWT)]
* https://datatracker.ietf.org/doc/html/rfc9068[RFC 9068 - JWT Profile for OAuth 2.0 Access Tokens]
* https://datatracker.ietf.org/doc/html/rfc8725[RFC 8725 - JSON Web Token Best Current Practices]
* https://datatracker.ietf.org/doc/html/rfc9449[RFC 9449 - OAuth 2.0 Demonstrating Proof of Possession (DPoP)]
* xref:../architecture.adoc[TokenSheriff Architecture Reference]
